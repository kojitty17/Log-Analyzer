# Log Analyzer

A simple log analysis tool that reads log files and highlights suspicious patterns such as repeated failed login attempts from the same IP.

v0 Features (for this month)

Read a log file line-by-line from disk.

Extract IP addresses and status/keywords.

Count failed login attempts per IP.

Print top offending IPs to console.
